apiVersion: kafka.strimzi.io/v1
kind: KafkaNodePool
metadata:
  name: kafka-pool
  namespace: hrmanagement
  labels:
    strimzi.io/cluster: kafka
spec:
  replicas: 1

  roles:
    - controller
    - broker

  storage:
    type: persistent-claim
    size: 2Gi
    deleteClaim: false