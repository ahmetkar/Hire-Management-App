services:
  mongodb:
    image: mongo:7
    container_name: hrmanagement-mongodb
    restart: unless-stopped
    command: ["mongod","--replSet","rs0","--bind_ip_all"]
    ports:
      - "27018:27017"
    volumes:
      - mongodb_data:/data/dbs
volumes:
  mongodb_data: